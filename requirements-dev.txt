# Development and CI dependencies.
# dbt is only needed to regenerate the fixture manifest; the checker itself
# reads JSON and never imports dbt.
pytest==7.4.4
ruff==0.6.9
dbt-core==1.9.4
dbt-snowflake==1.9.2
